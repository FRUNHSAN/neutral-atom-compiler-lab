中性原子量子编译器约束式工程分析 — 独立项目
https://github.com/FRUNHSAN/neutral-atom-compiler-lab	—— 从独立复现 IEEE TQE 2026 论文到约束式编译器重构，224 次机检运行，500 行独立编译器 NAC，14/14 benchmark 全验证
	2026.7 – 至今

技术架构：
• 全量论文复现：8 张图 7 张完成，14 个 TQE benchmark fidelity 分解独立验证，ZAP vs ZAC vs PowerMove vs Enola 四编译器对比，12 个 bug 逐根因记录
• 六桥敏感性测绘：将编译器的 6 个决策点形式化为桥（约束张力 + 消解策略），逐桥 swap 对比 fidelity Δ，快速排除 5 个平台期方向
• NAC 约束式编译器：~500 行独立实现，不调 ZAP API，scheduler + placer + router + simulator 四模块，参数全部来自 YAML boundary + bridge，无硬编码数值
• 增广拉格朗日联合优化求解器：连续权重 w∈[0,1] + 全局 slot 容量约束 Σ(1−w_i) ≤ slot_count + 梯度投影求解，替代 ZAP Eq.15 的 per-qubit 硬阈值
• 三编译器统一 fidelity 交叉验证：同一公式（Eq.4, 5 通道分解），ZAP (14/14 PASS) + ZAC (14/14 PASS) + NAC (14/14 PASS)
• 确定性可复现实验栈：zlib.crc32 替代 Python hash() 保证跨进程一致性，PYTHONHASHSEED 隔离验证（4 seed × 相同输出）
• 推理链系统：记录工程决策的"为什么不那样做"，3 条公开链（chains/）git 追踪，check.py 30 条规则自动校验链完整性

关键数字：
170→0 slot violation（hard_threshold 系统性失效 → AL 联合优化消除）
F_2q 与 ZAP 100% 一致（14/14），13/14 benchmark fidelity delta ≤ ±5%
|Δ| ≤ 0.0004 ZAP 复现 fidelity 与论文 Fig.7 一致
f_1q = 0.9997 隐藏参数发现（仅在架构 JSON，正文未给出——用错导致 F_wo_1q > 1）
7 实验脚本 | 5 张可视化图表 | 5 篇技术文档 | 3 条公开推理链

编译器无关性：
同一桥的分析方法可跨编译器迁移——Enola（单区架构）、ZAC（模拟退火迭代）、PowerMove（含 FTQC 编译）各自面对相同的物理约束张力，仅需替换编译器特定的 monkey-patch 注入点。

已知缺陷与待验证：
三条物理约束的 formal 字段未与 implementation 链路接通（YAML 中文注释 vs 代码执行）· fidelity 模型假设错误独立——五个物理过度简化（搬运-退相干耦合、阻塞概率性、f_tr 空间依赖性、SLM 不均匀、串扰空间依赖）待测绘
